import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    defaultContainer: {
        flex: 1, backgroundColor: 'black'
    },
    marginContainer:{
        marginTop: 24
    }
});